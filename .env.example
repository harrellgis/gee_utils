# Copy this file to `.env` and fill in your values. `.env` is gitignored.
# eetools.get_project() reads EE_PROJECT (then GOOGLE_CLOUD_PROJECT) as a fallback
# when no project is passed to eetools.initialize(project=...).
EE_PROJECT=your-ee-project-id
